<nav class="navbar navbar-expand-lg bg-body-dark" >
	<div class="container-fluid">
		<a class="navbar-brand" href="{{ path('app_home') }}"><img src="{{ asset('./assets/img/mon-logo.png') }}" alt="Logo" class="logoNav"></a>
        <section>
            <p class="titleNav">Le Domaine du Roi</p>
        </section>
		<button class="navbar-toggler colorButton" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
			<span class="navbar-toggler-icon"></span>
		</button>
		<section class="d-flex">
			<div class="collapse navbar-collapse" id="navbarSupportedContent">
				<ul class="navbar-nav me-auto mb-2 mb-lg-0">
					<li class="nav-item">
						<a class="nav-link liNav" aria-current="page" href="{{ path('app_home') }}">Accueil</a>
					</li>
					<li class="nav-item">
						<a class="nav-link liNav" href="{{ path('app_champagne') }}">Nos Champagnes</a>
					</li>
					{# <li class="nav-item">
						<a class="nav-link liNav" href="{{ path('app_champagne') }}">{{ userInfo.isLoggedIn ? "Mon Panier" : "" }}</a>
					</li> #}
					<li class="nav-item">
					<a class="nav-link liNav" href="{% if userInfo.user is null %}javascript:void(0){% else %} {{ path('app_panier') }} {% endif %}">
						Mon Panier
						{% if not userInfo.user is null %}
							<span class=" top-15 start-50 translate-middle badge rounded-pill bg-warning">
								{{ cartCount }}
							</span>
						{% endif %}
					</a>
				</li>
        			<li class="nav-item dropdown">
          				<a class="nav-link dropdown-toggle liNav" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">
            				{{ userInfo.isLoggedIn ? "Déconnexion" : "Inscription/Connexion" }}
          				</a>
					
          		<ul class="dropdown-menu liNav bg-dark">
				{% if userInfo.isSuperAdmin %}
				    <li><a class="dropdown-item" href="{{ path('app_membre_management') }}">Gestion des utilisateurs</a></li>

					<li><hr class="dropdown-divider"></li>

					<li><a class="dropdown-item" href="{{ path('app_admin') }}">Ajouter des produits</a></li>

					<li><a class="dropdown-item" href="{{ path('app_champagne') }}">Gestion des produits</a></li>

					<li><hr class="dropdown-divider"></li>

					<li><a class="dropdown-item" href="{{ path('app_admin_cepage') }}">Cepage</a></li>

					<li><a class="dropdown-item" href="{{ path('app_admin_typeProduit') }}">TypeProduit</a></li>

              		<li><hr class="dropdown-divider"></li>
              
              		<li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>

				{% elseif userInfo.isAdmin %}

					<li><a class="dropdown-item" href="{{ path('app_admin') }}">Ajouter des produits</a></li>

					<li><a class="dropdown-item" href="{{ path('app_champagne') }}">Gestion des produits</a></li>

              		<li><hr class="dropdown-divider"></li>
              
              		<li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>
					
				{% elseif userInfo.isLoggedIn %}
					<li><a class="dropdown-item" href="{{ path('app_membre') }}">Mon compte</a></li>

             		 <li><hr class="dropdown-divider"></li>

              		<li><a class="dropdown-item" href="{{ path('app_logout') }}">Déconnexion</a></li>

				{% else %}
            		<li>
						<a class="dropdown-item liNav" href="{{ path('app_register') }}">S'inscrire</a>
					</li>
					<li><hr class="dropdown-divider"></li>
            		<li>
						<a class="dropdown-item liNav" href="{{ path('app_login') }}">Se Connecter</a>
					</li> 
				{% endif %}           		
				</ul>

			</div>
		</section>
	</div>
</nav>
