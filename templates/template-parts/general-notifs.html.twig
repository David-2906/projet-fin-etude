<div class="notif">
	{% for label, messages in app.flashes(['success', 'warning', 'error']) %}
		{% for message in messages %}
			<div class="flash-{{ label }}">
				{{ message }}
			</div>
		{% endfor %}
	{% endfor %}
</div>


{# charger un flash #}
{# $this->session->getFlashBag()->add('notice', 'ATTENTION!!! :)') #}
{# 2eme facon: $this->addFlash('warning', 'on a gagné!!') #}