{% extends 'base.html.twig' %}

{% block title %}Mes infomations personnelles{% endblock %}

{% block body %}


<div class="container">

<h1 class="h3 text-center mb-4 liNav">Mes informations</h1>
        {% for message in app.flashes('success') %}
            <div class="alert alert-success mt-4">
                {{message}}
            </div>
        {% endfor %}

		<table class="table">
			<tbody>
				<tr class="text-color tab-respons">
					<th scope="col">Nom :</th>
					<td>{{ app.user.nom }}</td>
				</tr>
				<tr class="text-color tab-respons ">

					<th scope="col">Prénom :</th>
					<td>{{ app.user.prenom }}</td>
				</tr>
				<tr class="text-color tab-respons ">
					<th scope="col">Email :</th>
					<td>{{ app.user.email }}</td>
				</tr>
				<tr class="text-color tab-respons ">
					<th scope="col">Adresse :</th>
					<td>{{ app.user.adresse }}</td>
				</tr>
                <tr class="text-color tab-respons">
					<th scope="col">Ville :</th>
					<td>{{ app.user.ville }}</td>
				</tr>
                <tr class="text-color tab-respons">
					<th scope="col">Code Postal :</th>
					<td>{{ app.user.codePostal}}</td>
				</tr>
                <tr class="text-color tab-respons">
					<th scope="col">Date de Naissance :</th>
					<td>{{ app.user.dateNaissance|date('d-m-Y')}}</td>
				</tr>
                <tr class="text-color tab-respons">
					<th scope="col">Date de Naissance :</th>
					<td>{{ app.user.dateEnregistrement|date('d-m-Y')}}</td>
				</tr>
            </table>
                
{# {% if app.request.headers.get('User-Agent') matches '/(mobile)/i' %}
 <div class="d-flex justify-content-center">
                <button class="rounded bg-black mb-4 mt-4 w-25"><i class="fa-regular fa-pen-to-square" style="color: #1cc819;"></i><a class="btn" href="{{ path('app_modifier_mon_compte')}}"></a>
                <button class="rounded bg-black mb-4 mt-4 w-25"><i class="fa-regular fa-trash-can" style="color: #ec0404;"></i><a class="btn-compte" href="{{ path('app_supprimer_compte', {'id': app.user.id})}}"></a>
                </div>
                                <div class="d-flex justify-content-center">
<button class="rounded bg-black mb-4 mt-4 w-25"><a href='{{ path('app_membre')}}'>Retour</a></button>
                </div> #}
{# {% else %} #}
                <div class="d-flex justify-content-center">
                <button class="rounded bg-black mb-4 mt-4 w-25"><i class="fa-regular fa-pen-to-square" style="color: #1cc819;"></i><a class="btn-compte" href="{{ path('app_modifier_mon_compte')}}">Modifier mes informations</a>
                <button class="rounded bg-black mb-4 mt-4 w-25"><i class="fa-regular fa-trash-can" style="color: #ec0404;"></i><a class="btn-compte" href="{{ path('app_supprimer_compte', {'id': app.user.id})}}">Supprimer mon compte</a>
                </div>
                                <div class="d-flex justify-content-center">
<button class="rounded bg-black mb-4 mt-4 w-25"><a href='{{ path('app_membre')}}'>Retour</a></button>
                </div>
{# {% endif %} #}
</div>
{% endblock %}





